import { Router } from "express";

const router = Router();

router
  .post("/verify", () => {})
  .post("/solana", () => {})
  .post("/ethereum", () => {})
  .post("/request-message", () => {});

export default router;
